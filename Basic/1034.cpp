#include <algorithm>
#include <cmath>
#include <cstdio>
#include <cstring>
#include <iostream>
#include <queue>
#include <string>
#include <vector>

using namespace std;

int a1, b1, a2, b2;



int main() {
	ios::sync_with_stdio(false);
	scanf("%d/%d %d/%d", &a1, &b1, &a2, &b2);
	// cout << a1 << ' ' << b1 << ' ' << a2 << ' ' << b2 << endl;
		
	
	return 0;
}
